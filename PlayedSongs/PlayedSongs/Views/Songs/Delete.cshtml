@model PlayedSongs.Models.Song

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Song</legend>

    <div class="display-label">Name</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Name)
    </div>

    <div class="display-label">Artist</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Artist)
    </div>

    <div class="display-label">Album</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Album)
    </div>

    <div class="display-label">Learned</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Learned)
    </div>

    <div class="display-label">TabOrigin</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.TabOrigin)
    </div>

    <div class="display-label">TabPath</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.TabPath)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
