using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Xml.Linq;

namespace DataParser
{
    class Program
    {
        static void Main(string[] args)
        {
            //var data = XElement.Load("Fringe.xml");
            
            //var showId = data.Element("Series").Element("id").Value;

            //var episodes = XElement.Load("Fringe.xml").Elements("Episode").GroupBy(e => e.Element("seasonid").Value);

            //foreach (var season in episodes)
            //{
            //    Console.WriteLine("Season: " + season.Key);
            //    Console.WriteLine("Season number: " + season.First().Element("Combined_season").Value);
            //    Console.WriteLine("Episodes: " + season.Count());
            //    Console.WriteLine(Environment.NewLine);
            //}

            //Console.ReadKey();
        }
    }
}
